// Empty test-only stub for Next's `server-only` virtual module.
// In production, Next throws when this module is imported from a Client
// Component bundle; in Vitest there is no client/server split, so importing
// it is safe and the warning behavior is unnecessary.
export {};
